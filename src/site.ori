{
  assets

  (data): airtable.ori/

  // Create virtual image files that retrieve the corresponding URL
  images = map(data, { key: =_/filename, value: =fetch(_/url) })

  index.html = index.ori.html(data)

  team = map(data, person.ori.html)
}
